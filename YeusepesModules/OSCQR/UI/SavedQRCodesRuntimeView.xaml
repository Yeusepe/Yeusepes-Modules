<UserControl x:Class="YeusepesModules.OSCQR.UI.SavedQRCodesRuntimeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:YeusepesModules.OSCQR.UI"
             mc:Ignorable="d">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </UserControl.Resources>
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel>
            <TextBlock Text="Detected Codes" 
                       FontWeight="Bold" 
                       Foreground="White" 
                       FontSize="16" 
                       Margin="5,5,5,5"/>
            <ItemsControl ItemsSource="{Binding DetectedCodes}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Margin="5">
                            <TextBlock Text="{Binding DisplayText}"
                                       Foreground="White"
                                       FontSize="14"
                                       FontWeight="Bold"
                                       Margin="0,0,0,2"/>
                            <TextBlock Text="{Binding TypeInfo}"
                                       Foreground="LightGray"
                                       FontSize="12"
                                       Margin="0,0,0,2"
                                       Visibility="{Binding HasTypeInfo, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                            <TextBlock Text="{Binding Url}"
                                       Foreground="LightBlue"
                                       FontSize="12"
                                       TextDecorations="Underline"
                                       Cursor="Hand">
                                <TextBlock.InputBindings>
                                    <MouseBinding MouseAction="LeftClick"
                                                  Command="{Binding DataContext.OpenLinkCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                  CommandParameter="{Binding Url}" />
                                </TextBlock.InputBindings>
                            </TextBlock>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
